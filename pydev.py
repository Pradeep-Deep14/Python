abc={1,2,3}
abc2=abc.copy()
abc2.add(2)
print(abc2)

# //sets in Python are unordered collections of unique elements, so duplicates are not allowed. Since 2 is already in abc, adding it again to abc2 doesn't change the set's contents.